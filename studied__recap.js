const a=5;
// const isNicoFat= true;

isNicoFat= false;
// const 사용했기 때문에 'false'로 바꾸는 것은 오류를 만든다.
// so using 'let' is more better!!!


let isNicoFat = true;
// u can use 'var' but it doesn't have a meaning 
// so do not use it!!

const b = true;
// true and false are booleans 


const c =null;
// null litterally means nnothing.... 변수에 할당되는 값이다.

let hello;
console.log(hello);
// undefinde는 변수에 값을 부여하지 않은 상태. no type

const dyas = [];
// this is how to creat an array

const days = [1,2,false,true,null, undefined,"text", me];

console.log(days[2]);
days = "true";
console.log(days[2]);
days.push("meet");